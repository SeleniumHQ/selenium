1) Config or property file for default host port number

2) More sample scripts for using driven selenium

3) Use ActiveState's PerlApp to create exe version of CGIProxy
	This removes the dependency of needing Perl installed.

4) Use Py2Exe to create exe version of selenium_server.py
	This removes the dependency of needing Python installed

5) Use NSIS to create Windows installer of the above two exes.
	Makes uses Selenium almost a "one-click install" application.

6) Creating Linux, Mac "exes" and installers.

7) Adding client proxy server (aka "The Squid/Squirm Proxy")
	Mentioned on the developer list. This is an alternative approach
	to getting around the same-origin policy for testing remote
	server.

