# Used by Qodo Review v2

[github_app]
pr_commands = [
    "/agentic_describe",
    "/agentic_review",
]
handle_push_trigger = true
push_commands = [
    "/agentic_review",
]

[agentic_review]
extra_instructions = "Look at the guidelines in .github/pr_review.md when evaluating this PR."
