schema {
  query: GridQuery
}

scalar Uri

enum Status {
  UP
  DRAINING
  UNAVAILABLE
}

type Session {
  id: ID!
}

type Node {
  id: ID!
  uri: Uri!
  status: Status!
  maxSession: Int!
  capabilities: String!
}

type GridQuery {
  grid: Grid!
}

type Grid {
  uri: Uri!
  nodes: [Node!]!
  sessions: [Session]
  sessionCount: Int!
  totalSlots: Int
  usedSlots: Int
}
