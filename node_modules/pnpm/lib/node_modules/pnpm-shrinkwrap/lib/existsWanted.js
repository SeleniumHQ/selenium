"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const fs = require("fs");
const path = require("path");
const constants_1 = require("./constants");
exports.default = (pkgPath) => new Promise((resolve, reject) => {
    fs.access(path.join(pkgPath, constants_1.WANTED_SHRINKWRAP_FILENAME), (err) => {
        if (!err) {
            resolve(true);
            return;
        }
        if (err.code === 'ENOENT') {
            resolve(false);
            return;
        }
        reject(err);
    });
});
//# sourceMappingURL=existsWanted.js.map