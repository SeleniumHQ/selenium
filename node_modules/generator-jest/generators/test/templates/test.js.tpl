const assert = require('assert');
const <%- name %> = require('<%- filepath %>');

describe('<%- name %>', () => {
  it('has a test', () => {
    assert(false, '<%- name %> should have a test');
  });
});
