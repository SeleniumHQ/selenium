<?xml version="1.0" encoding="UTF-8"?>
<project name="MainActivity">

<!-- The local.properties file is created and updated by the 'android'
     tool.
     It contains the path to the SDK. It should *NOT* be checked into
     Version Control Systems. -->
    <property file="local.properties" />

    <!-- The build.properties file can be created by you and is never touched
         by the 'android' tool. This is the place to change some of the
         default property values used by the Ant rules.
         Here are some properties you may want to change/update:

         source.dir
             The name of the source directory. Default is 'src'.
         out.dir
             The name of the output directory. Default is 'bin'.

         Properties related to the SDK location or the project target should
         be updated using the 'android' tool with the 'update' action.

         This file is an integral part of the build system for your
         application and should be checked into Version Control Systems.

         -->
    <property file="build.properties" />

    <!-- The default.properties file is created and updated by the 'android'
         tool, as well as ADT.
         This file is an integral part of the build system for your
         application and should be checked into Version Control Systems. -->
    <property file="default.properties" />

    <!-- Custom Android task to deal with the project target, and import the
         proper rules.
         This requires ant 1.6.0 or above. -->
    <path id="android.antlibs">
      <pathelement path="${sdk.dir}/tools/lib/anttasks.jar" />
      <pathelement path="${sdk.dir}/tools/lib/sdklib.jar" />
      <pathelement path="${sdk.dir}/tools/lib/androidprefs.jar" />
      <pathelement path="${sdk.dir}/tools/lib/apkbuilder.jar" />
      <pathelement path="${sdk.dir}/tools/lib/jarutils.jar" />
    </path>

    <property name="root.dir" value="../../"/>
    <property name="third_party.location" value="${root.dir}/third_party"/>
    <property name="build.location" value="${root.dir}/build" />
    <property name="javascript.atoms" value="javascript/atoms"/>
    <property name="webdriver.atoms.inject" value="javascript/webdriver-atoms/inject"/>
    <property name="webdriver.atoms" value="javascript/webdriver-atoms"/>
    <property name="go" value="./go"/>
    <property name="res.raw" value="res/raw"/>

    <copy file="${build.location}/java/server/src/org/openqa/jetty/jetty.jar" tofile="libs/jetty.jar" />
    <copy file="${build.location}/java/client/src/org/openqa/selenium/base.jar" tofile="libs/base.jar" />
    <copy file="${build.location}/java/client/src/org/openqa/selenium/internal/codecs.jar" tofile="libs/codecs.jar" />
    <copy file="${build.location}/java/client/src/org/openqa/selenium/webdriver-api.jar" tofile="libs/webdriver-api.jar" />
    <copy file="${build.location}/java/client/src/org/openqa/selenium/remote/common.jar" tofile="libs/webdriver-common.jar" />
    <copy file="${build.location}/java/client/src/org/openqa/selenium/remote/remote.jar" tofile="libs/webdriver-client-remote.jar" />
    <copy file="${build.location}/java/client/src/org/openqa/selenium/browserlaunchers/launcher-utils.jar" tofile="libs/launcher-utils.jar" />
    <copy file="${build.location}/java/client/src/org/openqa/selenium/support/events/events.jar" tofile="libs/events.jar" />
    <copy file="${build.location}/java/server/src/org/openqa/selenium/remote/server/server.jar" tofile="libs/remote-wdserver.jar" />
    <copy file="${build.location}/java/server/src/org/openqa/selenium/server/logging.jar" tofile="libs/remote-logging.jar" />
    <copy file="${third_party.location}/java/guava-libraries/guava-r09.jar" todir="libs/"  />
    <copy file="${third_party.location}/java/jetty/jetty-lite-7.2.0-repackaged-for-android.jar" todir="libs/" />
    <copy file="${third_party.location}/java/json/json-20080701.jar" todir="libs/" />
    <copy file="${third_party.location}/java/junit/junit-dep-4.10.jar" todir="libs/" />
    <copy file="${build.location}/java/client/src/org/openqa/selenium/io/io.jar" todir="libs" />

    <taskdef name="setup"
        classname="com.android.ant.SetupTask"
        classpathref="android.antlibs" />

    <!-- Building the atoms before the apk compile all gos at once -->
    <target name="-pre-compile" depends="shouldBuildAtoms" unless="atomsBuild.notRequired">
        <exec executable="${go}" dir="${root.dir}">
            <arg value="//${webdriver.atoms.inject}:webdriver-inject"/>
            <arg value="//${webdriver.atoms.inject}:active_element:android"/>
            <arg value="//${webdriver.atoms.inject}:clear:android"/>
            <arg value="//${webdriver.atoms.inject}:default_content:android"/>
            <arg value="//${webdriver.atoms}:execute_script:android"/>
            <arg value="//${webdriver.atoms.inject}:find_element:android"/>
            <arg value="//${webdriver.atoms.inject}:find_elements:android"/>
            <arg value="//${webdriver.atoms.inject}:frame_by_id_or_name:android"/>
            <arg value="//${webdriver.atoms.inject}:frame_by_index:android"/>
            <arg value="//${webdriver.atoms.inject}:get_attribute_value:android"/>
            <arg value="//${webdriver.atoms.inject}:get_size:android"/>
            <arg value="//${webdriver.atoms.inject}:get_text:android"/>
            <arg value="//${webdriver.atoms.inject}:get_top_left_coordinates:android"/>
            <arg value="//${webdriver.atoms.inject}:get_value_of_css_property:android"/>
            <arg value="//${webdriver.atoms.inject}:is_displayed:android"/>
            <arg value="//${webdriver.atoms.inject}:is_enabled:android"/>
            <arg value="//${webdriver.atoms.inject}:is_selected:android"/>
            <arg value="//${webdriver.atoms.inject}:submit:android"/>

            <arg value="//${webdriver.atoms.inject}:get_local_storage_keys:android"/>
            <arg value="//${webdriver.atoms.inject}:set_local_storage_item:android"/>
            <arg value="//${webdriver.atoms.inject}:get_local_storage_item:android"/>
            <arg value="//${webdriver.atoms.inject}:remove_local_storage_item:android"/>
            <arg value="//${webdriver.atoms.inject}:clear_local_storage:android"/>
            <arg value="//${webdriver.atoms.inject}:get_local_storage_size:android"/>

            <arg value="//${webdriver.atoms.inject}:get_session_storage_keys:android"/>
            <arg value="//${webdriver.atoms.inject}:set_session_storage_item:android"/>
            <arg value="//${webdriver.atoms.inject}:get_session_storage_item:android"/>
            <arg value="//${webdriver.atoms.inject}:remove_session_storage_item:android"/>
            <arg value="//${webdriver.atoms.inject}:clear_session_storage:android"/>
            <arg value="//${webdriver.atoms.inject}:get_session_storage_size:android"/>
        </exec>

        <copy file="${build.location}/${webdriver.atoms.inject}/active_element_android.js" todir="${res.raw}"/>
        <copy file="${build.location}/${webdriver.atoms.inject}/clear_android.js" todir="${res.raw}"/>
        <copy file="${build.location}/${webdriver.atoms.inject}/default_content_android.js" todir="${res.raw}"/>
        <copy file="${build.location}/${webdriver.atoms}/execute_script_android.js" todir="${res.raw}"/>
        <copy file="${build.location}/${webdriver.atoms.inject}/find_element_android.js" todir="${res.raw}"/>
        <copy file="${build.location}/${webdriver.atoms.inject}/find_elements_android.js" todir="${res.raw}"/>
        <copy file="${build.location}/${webdriver.atoms.inject}/frame_by_id_or_name_android.js" todir="${res.raw}"/>
        <copy file="${build.location}/${webdriver.atoms.inject}/frame_by_index_android.js" todir="${res.raw}"/>
        <copy file="${build.location}/${webdriver.atoms.inject}/get_attribute_value_android.js" todir="${res.raw}"/>
        <copy file="${build.location}/${webdriver.atoms.inject}/get_size_android.js" todir="${res.raw}"/>
        <copy file="${build.location}/${webdriver.atoms.inject}/get_text_android.js" todir="${res.raw}"/>
        <copy file="${build.location}/${webdriver.atoms.inject}/get_top_left_coordinates_android.js" todir="${res.raw}"/>
        <copy file="${build.location}/${webdriver.atoms.inject}/get_value_of_css_property_android.js" todir="${res.raw}"/>
        <copy file="${build.location}/${webdriver.atoms.inject}/is_displayed_android.js" todir="${res.raw}"/>
        <copy file="${build.location}/${webdriver.atoms.inject}/is_enabled_android.js" todir="${res.raw}"/>
        <copy file="${build.location}/${webdriver.atoms.inject}/is_selected_android.js" todir="${res.raw}"/>
        <copy file="${build.location}/${webdriver.atoms.inject}/submit_android.js" todir="${res.raw}"/>

        <copy file="${build.location}/${webdriver.atoms.inject}/get_local_storage_keys_android.js" todir="${res.raw}"/>
        <copy file="${build.location}/${webdriver.atoms.inject}/set_local_storage_item_android.js" todir="${res.raw}"/>
        <copy file="${build.location}/${webdriver.atoms.inject}/get_local_storage_item_android.js" todir="${res.raw}"/>
        <copy file="${build.location}/${webdriver.atoms.inject}/remove_local_storage_item_android.js" todir="${res.raw}"/>
        <copy file="${build.location}/${webdriver.atoms.inject}/clear_local_storage_android.js" todir="${res.raw}"/>
        <copy file="${build.location}/${webdriver.atoms.inject}/get_local_storage_size_android.js" todir="${res.raw}"/>

        <copy file="${build.location}/${webdriver.atoms.inject}/get_session_storage_keys_android.js" todir="${res.raw}"/>
        <copy file="${build.location}/${webdriver.atoms.inject}/set_session_storage_item_android.js" todir="${res.raw}"/>
        <copy file="${build.location}/${webdriver.atoms.inject}/get_session_storage_item_android.js" todir="${res.raw}"/>
        <copy file="${build.location}/${webdriver.atoms.inject}/remove_session_storage_item_android.js" todir="${res.raw}"/>
        <copy file="${build.location}/${webdriver.atoms.inject}/clear_session_storage_android.js" todir="${res.raw}"/>
        <copy file="${build.location}/${webdriver.atoms.inject}/get_session_storage_size_android.js" todir="${res.raw}"/>
    </target>

    <target name="shouldBuildAtoms" unless="atomsBuild.notRequired">
        <uptodate property="atomsBuild.notRequired" targetfile="${build.location}/${webdriver.atoms.inject}/webdriver-inject.js" >
          <srcfiles dir= "${root.dir}/${javascript.atoms}"/>
          <srcfiles dir= "${root.dir}/${webdriver.atoms.inject}"/>
          <srcfiles dir= "${root.dir}/${webdriver.atoms}"/>
        </uptodate>
    </target>

<!-- extension targets. Uncomment the ones where you want to do custom work
     in between standard targets -->
<!--
    <target name="-pre-build">
    </target>

    [This is typically used for code obfuscation.
     Compiled code location: ${out.classes.absolute.dir}
     If this is not done in place, override ${out.dex.input.absolute.dir}]
    <target name="-post-compile">
    </target>
-->


    <!-- Execute the Android Setup task that will setup some properties
         specific to the target, and import the build rules files.

         The rules file is imported from
            <SDK>/platforms/<target_platform>/ant/ant_rules_r#.xml

         To customize existing targets, there are two options:
         - Customize only one target:
             - copy/paste the target into this file, *before* the
               <setup> task.
             - customize it to your needs.
         - Customize the whole script.
             - copy/paste the content of the rules files (minus the top node)
               into this file, *after* the <setup> task
             - disable the import of the rules by changing the setup task
               below to <setup import="false" />.
             - customize to your needs.
    -->
    <setup />
</project>
