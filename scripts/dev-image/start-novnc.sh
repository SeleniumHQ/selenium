#!/usr/bin/env bash
#
# IMPORTANT: Change this file only in directory NodeBase!

#/opt/bin/noVNC/utils/launch.sh --listen ${NO_VNC_PORT} --vnc localhost:${VNC_PORT}
echo "noVNC won't start because SE_START_NO_VNC is false."
