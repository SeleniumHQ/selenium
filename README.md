# Selenium WebDriver (python 2.4)

This is fork of SeleniumHQ/selenium, where is changed only py module (Python WebDriver). This fork  supports Python 2.4 (for example RHEL5 and Centos 5). See README in py module.
